language: java

sudo: false

jdk:
  - openjdk8

addons:
  sonarcloud:
    organization: swce

script:
  - sonar-scanner

install:
  - ./gradlew build

cache:
  directories:
    - $HOME/.m2/repository
    - $HOME/.sonar/cache

notifications:
  email:
    - lchayoun@gmail.com
